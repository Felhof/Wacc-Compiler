stages:
        - build
        - test

job 1:
        stage: build
        script:
          - make
          - mvn compile
job 2:
        stage: test
        script:
          - make
          - mvn test # mvn test -Dgroups="<Test Category>"
        only:
          - master # only or except: - <name of branch>
