stages:
- build
- test

job 1:
  stage: build
  script: make
job 2:
  stage: test
  script:
  - make
  - mvn test -Dtest=SymbolTableUnitTests
  - mvn test '-Dtest=SemanticInvalidTests#SemanticIf+SemanticScope
                     +SemanticVariables+SemanticWhile+SemanticArray'
    #- mvn test '-Dtest=!InvalidTests#Expression+Variables' # mvn test
    #      -Dgroups="<Test Category>"
    #only:
    #  - master  # only or except: - <name of branch>
