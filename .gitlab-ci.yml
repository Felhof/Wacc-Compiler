stages:
        - build
        - test

job 1:
        stage: build
        script:
          - make
          - mvn compile
job 2:
        stage: test
        script:
          - make
          - mvn test -Dgroups="Basic"
        only:
          - 2-set-yml-file-to-automate-test-pipeline
