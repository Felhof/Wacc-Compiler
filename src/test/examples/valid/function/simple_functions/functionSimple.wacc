# a simple function definition and call

# Output:
# 0

# Program:

begin
  bool f(bool b, int y, int v) is
    return b
  end

  int v = 16 ;
  bool nb = true ; 
  int h = 12 ;
  bool x = call f(nb, v, h)
end
